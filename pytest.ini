[pytest]
testpaths = tests
markers =
    gui: requires PyQt/PyVista GUI dependencies
    mpi: requires mpirun and mpi4py
    pymeshlab: requires pymeshlab
    analytical: validates against analytical or known synthetic truth
    current_gap: documents a known gap in the current implementation
addopts = -ra
